import styled from 'styled-components';

export const Container = styled.div`
    display: flex;
    align-items: center;
`;

export const InputSearch = styled.input`
    height: 40px;
    border: none;
    background: #F5F5F7;
    border-radius: 8px;
    outline: none;
    padding: 0 20px;
`;

export const NotificationButton = styled.img`
    width: 23.83px;
    height: 26px;
    margin: 0 20px;
`;

export const UserProfile = styled.img`
    width: 40px;
    height: 40px;
    border-radius: 8px;
    object-fit: cover;
`;

export const UserSettings = styled.div`
    
`;